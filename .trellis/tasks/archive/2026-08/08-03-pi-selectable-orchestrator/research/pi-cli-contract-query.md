# Pi Agent CLI contract research

Identify the current official Pi coding agent CLI contract needed to invoke it as a non-interactive CCG backend.

Return source-backed answers for:

- official package/repository identity and executable name;
- non-interactive prompt syntax;
- output formats, especially machine-readable JSON or streaming JSON;
- session/resume syntax and session identifier output;
- working-directory behavior;
- tool/permission flags suitable for an autonomous orchestration sub-task;
- Windows command/shim considerations;
- the smallest safe invocation that returns one final text result.

Use official sources only. Do not install, authenticate, or invoke Pi.
