interface:
  display_name: "ChatGPT Pro Browser Bridge"
  short_description: "Delegate to Pro in external Chrome without model polling"
  default_prompt: "Use $chatgpt-pro-sidebar with agent-browser-cli V2 to delegate this bounded task through the approved external Chrome tab and monitor it with local RootWait."
policy:
  allow_implicit_invocation: true
