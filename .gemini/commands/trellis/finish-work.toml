description = "Trellis: finish-work"

prompt = """
Follow Request Triage and Phase 3.5 in `.trellis/workflow.md`. Fast-lane requests finish with their result and applicable quality checks; do not create task artifacts, archives or journals for them.

For the current structured task, load `get_context.py --mode phase --step 3.5 --platform gemini` and follow the canonical completion contract. Preserve hard user gates and unrelated dirty work. Completion does not force a commit; archive the current completed task with `--no-commit` and keep branch validation. Add a journal only for a concrete handoff need. Do not scan or clean up other tasks.

"""
