description = "Trellis: continue"

prompt = """
Read Request Triage in `.trellis/workflow.md` first. Fast-lane requests need no task lifecycle. For structured work, reuse the matching task. Complex tasks require `prd.md`, `design.md`, and `implement.md`. Present completed planning artifacts for review and wait for explicit implementation approval; task-creation consent alone does not authorize implementation. Resume an approved implementation at its first unfinished step.

Load only missing context through `get_context.py --mode phase --step <X.Y> --platform gemini`. Follow that canonical guidance, including required artifacts, project lint/type-check/tests, coverage requirements, dispatch authorization and pending hard user gates. Do not duplicate the workflow into a second checklist or force a commit before completion.

"""
